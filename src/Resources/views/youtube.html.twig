{% block stream %}
    <iframe src="//www.youtube.com/embed/{{ stream.identifier }}" id="live_embed_player_flash" allowfullscreen width="{{ width }}" height="{{ height }}" frameborder="0"></iframe>
{% endblock stream %}
