darkanakin41_stream:
    stream_class: "AppTestBundle\\Entity\\Stream"
    category_class: "AppTestBundle\\Entity\\StreamCategory"
    platform:
        google:
            developper_key: '%env(GOOGLE_APPLICATION_KEY)%'
            referer: 'scoopturn.com'
        twitch:
            client_id: '%env(TWITCH_CLIENT_ID)%'
            client_secret: '%env(TWITCH_CLIENT_SECRET)%'
